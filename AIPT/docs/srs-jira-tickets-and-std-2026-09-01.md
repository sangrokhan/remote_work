# AIPT — SRS(JIRA 티켓 초안) & STD(테스트 설계서) — 구현 완료 기능 기준

- 작성일: 2026-09-01 (수정판 — 미구현 갭이 아닌 **이미 구현·실측 검증된 기능**을 대상으로 재작성)
- 근거 문서: `DESIGN.md` §4.5~§4.10, `ARCHITECTURE.md` §2~§7
- 범위: 실제 코드로 구현이 완료되고 테스트/실측으로 검증된 6개 핵심 기능을 JIRA 티켓 후보(SRS 대체)와
  그에 대응하는 회귀 테스트 케이스(STD)로 정리한다. (직전에 드린 "미해결 갭" 버전을 대체)

## 1. SRS — JIRA 티켓 초안 (요구사항서 대체, 구현 완료 기능)

| Epic Name | Summary | Feature Type | Description | Constraint | Validation Method | Validation |
|---|---|---|---|---|---|---|
| 3-Backend 통합 아키텍처 | PublicAI/Mock/LocalLLM 3개 backend 공통 프로토콜 구현 | New Feature | `Backend` 프로토콜(connect/send_turn/close)로 3개 backend(PublicAIBackend: 실제 Gemini/OpenAI 10개 arm, MockBackend: 고정 byte-size/Q&A fixture 재생, LocalLLMBackend: llama.cpp/vLLM 앞단 engine gateway 경유)를 단일 인터페이스로 통합 구현. `aipt/web`이 backend 목록/준비 상태를 동적으로 수집해 노출한다. | PublicAI만 Network Gateway를 경유하지 않음(이미 실제 인터넷이 역할 수행). 소켓 레벨 계측 가능한 요청 방식(SDK 대신 requests 기반)을 써야 함. | `tests/test_backends_base.py`(레지스트리 등록/조회) + `tests/backends/`(15개 파일, 163개 테스트, 프로토콜 준수/arm별 body 빌드) | `pytest -m "not live" tests/backends/ tests/test_backends_base.py` 전량 pass(163+11건), 3개 backend 모두 FastAPI TestClient로 실행 스모크 통과 |
| Network Gateway L3 트래픽 셰이핑 | client-link-only netem 기반 Network Gateway 구현 | New Feature | Mock/LocalLLM backend 경로에 `tc netem`으로 지연/지터/손실/재정렬을 주입하는 순수 L3 IP 포워딩 컨테이너 구현 완료. client_iface(net-client)에는 사용자가 고른 프로파일(clean/wired/wireless/custom)을 양방향 적용하고, ingress 방향은 IFB(Intermediate Functional Block)로 리다이렉트해 우회 없이 shaping. backend_iface는 항상 고정 `ETHERNET_BASELINE`만 적용. | Gateway는 TCP 상태/페이로드를 들여다보지 않는 순수 L3 forwarding이어야 함(`NET_ADMIN` + `ip_forward=1`만 사용). 런타임 프로파일 전환이 기존 keep-alive 연결에도 즉시 반영되어야 함. | `tests/gateway/`(4개 파일, 65개 테스트 — 프로파일 값 정의, tc 명령 구성 subprocess mock, API 라우트, client-link-only L3 forwarding 로직) | `pytest -m "not live" tests/gateway/` 65건 전량 pass, 실컨테이너에서 프로파일별 RTT가 설정값 근방으로 실측됨 |
| engine Gateway 요청 dedup 캐싱 | LocalLLM 멀티턴 요청 leaf-hash 중복 제거 캐싱 구현 | New Feature | 멀티턴 대화에서 매 턴 재전송되는 `messages` 텍스트를 TCP 세션(커넥션) 단위로 hash 치환해 전송량 절감. 클라이언트측(`Gateway.send()`)이 leaf 순회 후 이미 본 값을 hash로 치환, 서버측(`docker/engine_gateway.py` L7 sidecar, 포트 40079)이 세션 캐시로 원복. 캐시 미스 시 HTTP 409 + 1회 재전송. | `local_llm` backend 전용, 기본값 off, `X-AIPT-Cache: enable` 헤더로 opt-in. 200 bytes 미만 값은 캐싱 대상 제외(임계값). | `scripts/measure_perf_cache_savings.py`로 실제 4-service docker-compose 토폴로지(web→Network Gateway→engine Gateway→llama-server) 위에서 off/on 실측 비교 + `scripts/_smoketest_*.py` 4종(standalone assert) | 20턴 누적 기준 요청 payload 87.2% 절감(529,002→67,592 bytes), 실제 wire 전송량 86.3% 절감(533,942→72,952 bytes) 실측 확인, 4개 smoketest 스크립트 전량 pass |
| Run Store 영속화 | 전체 backend 실행 결과 디스크 영속화 | Enhancement | `aipt/web/store.py`가 `RUN_STORE_DIR`(기본 `data/runs/`)에 모든 backend(PublicAI/Mock/LocalLLM)의 모든 run을 JSON으로 디스크 영속화하도록 구현 완료(2026-08-27). 재시작해도 과거 run 목록이 유지됨. | 기존 "인메모리 최근 50개만 유지" 철학에서 전환된 결정 — 디스크 용량 관리는 후속 과제로 별도 존재. | `tests/web/`(5개 파일, 51개 테스트 — FastAPI TestClient 라우트 스모크, 세션 store 포함) | `pytest -m "not live" tests/web/` 51건 pass, 웹 프로세스 재기동 후 `GET /api/runs`로 과거 run 목록 조회 시 재시작 이전 실행 결과가 유지됨을 확인 |
| 적응형 cwnd 계측 인프라 | 별도 프로세스 + RTT 비례 적응형 샘플링 주기 구현 | New Feature | `aipt/core/cwnd.py`의 `Monitor`가 Python 스레드가 아닌 완전히 별도의 OS 프로세스(`native/cwnd_monitor.c`)를 기동해 자체 클록으로 netlink `sock_diag`를 폴링, GIL/이벤트루프 지연의 샘플링 왜곡을 원천 차단. 샘플링 주기는 고정값이 아니라 `interval_ms = max(1, rtt_ms / K)`로 경로 RTT에 비례해 계산(적응형), 물리적 하한(1ms) 아래는 `floor_clamped` + `measurement_confidence: "degraded"`로 명시 보고. | 짧은 RTT 경로(Mock backend clean 프로파일 등)에서 고정 주기를 쓰면 슬로우스타트 burst를 샘플러가 놓칠 수 있음 — 반드시 적응형이어야 함. | `tests/core/`(9개 파일, 170개 테스트 — cwnd reset 판정, AppArmor 감지, interval_from_rtt, timestamp_source 판별 등) | `pytest -m "not live" tests/core/` 170건 pass, idle 후 IW 리셋과 loss recovery가 서로 다른 이벤트로 정확히 구분 판정됨을 단위 테스트로 확인 |
| QUIC idle-probe 혼잡제어 스파이크 | QUIC 유저스페이스 congestion control로 idle-probe RTT 사전 감지·cwnd 조정 구현 | New Feature | aioquic 표준 Reno 위에 `IdleProbeCongestionControl` 구현(`aipt/backends/quic_mock/congestion.py`): idle 진입 시 `mark_idle_probe_sent()`로 pre-idle RTT 기록, 이후 probe PING의 ACK(`on_rtt_measurement()`)로 RTT 증가율을 계산해 사전에 cwnd를 축소(`MAX_REACTED_GROWTH=0.5` 캡). `spike_runner.py`가 실제 Gateway netem 프로파일 위에서 baseline(reno) vs idle_probe A/B 실행. | TCP 커널 레벨에서는 이 방식이 구조적으로 불가능함(RTO 기반 keepalive는 Karn's algorithm에서 RTT 샘플로 배제됨, `tcp_congestion_ops`는 능동 송신 권한 없음)을 커널 소스로 확인 후 QUIC(유저스페이스, `send_ping()` 공개 API)으로 전환한 설계 — mock 전용, 아직 웹 UI/`RunRequest`에는 미연결. | `tests/backends/quic_mock/`(4개 파일, 11개 테스트 — 프로토콜 준수, congestion 실험 파라미터, live e2e) + 실컨테이너 A/B 실측 | `3g` 프로파일(delay 150±40ms, loss 1%)에서 idle_probe가 매 턴 RTT 증가(11.7~19.4%)를 실제로 감지해 cwnd를 사전 축소(예: 3188→2643)함을 실측 확인, `clean` 프로파일에서는 두 알고리즘 차이가 노이즈 수준(예상대로) |

## 2. STD — 테스트 케이스 (구현 완료 기능 회귀 검증)

| Test ID | Description | Precondition | Test Step | Expected Results |
|---|---|---|---|---|
| TC-AIPT-11 | 3개 backend 공통 프로토콜 준수 및 레지스트리 동작 검증 | venv 활성화, `tests/backends/`, `tests/test_backends_base.py` 존재 | 1) `pytest -m "not live" tests/test_backends_base.py -v` 실행 2) `pytest -m "not live" tests/backends/ -v` 실행 3) 미등록 backend 이름 조회 시 예외 확인 | `tests/test_backends_base.py` 11건, `tests/backends/` 163건 모두 pass, 미등록 이름 조회 시 명시적 예외 발생 |
| TC-AIPT-12 | Network Gateway client-link-only netem 프로파일 적용 검증 | docker-compose로 `gateway`/`mock-server` 컨테이너 기동, `wireless` 프로파일 정의 존재 | 1) `POST /gateway/profile` 로 `wireless` 프로파일 설정 2) mock backend로 실험 실행 3) `aipt.core.probe` 또는 pcap SYN-ACK로 RTT 측정 4) backend_iface(net-backend) 구간은 별도로 `ETHERNET_BASELINE` 고정 여부 확인 | client_iface 구간 RTT가 `wireless` 프로파일 delay 설정값 근방으로 관측되고, backend_iface 구간은 프로파일 선택과 무관하게 baseline(약 1ms)으로 유지됨 |
| TC-AIPT-13 | engine Gateway 캐싱 트래픽 절감 실측 재현 검증 | docker-compose 4-service 토폴로지 기동, `records/perf_short_smoketest.json` 존재 | 1) `X-AIPT-Cache: enable` off로 `scripts/measure_perf_cache_savings.py` 실행(baseline) 2) on으로 재실행(cached) 3) `data/runs/cache_savings_multiturn.csv`에서 턴별 `req_payload_bytes`/`wire_sent` 비교 4) `scripts/_smoketest_cache_miss_recovery.py`, `scripts/_smoketest_gateway_409_path.py` 실행 | 20턴 누적 payload 절감률 87.2%, wire 전송량 절감률 86.3% 근방 재현, 캐시 미스(409) 발생 시 클라이언트가 자기 캐시로 원본 복원 후 1회 재전송에 성공, 두 smoketest 스크립트 모두 pass |
| TC-AIPT-14 | Run store 디스크 영속화 및 재시작 후 이력 유지 검증 | `RUN_STORE_DIR`(`data/runs/`) 쓰기 가능, `aipt/web` 프로세스 기동 가능 | 1) Mock backend로 실험 1건 실행 2) `GET /api/runs`로 run 목록에 포함 확인 3) `aipt/web` 프로세스 재시작 4) 재시작 후 `GET /api/runs` 재호출 | 재시작 전후 모두 방금 실행한 run이 `data/runs/`에 JSON으로 남아있고 `GET /api/runs` 응답에 포함됨(PublicAI뿐 아니라 Mock/LocalLLM도 동일하게 영속화됨) |
| TC-AIPT-15 | 적응형 cwnd 샘플링 주기 및 idle-reset 판정 정확성 검증 | netlink `sock_diag` 접근 가능한 환경(`@pytest.mark.live` 대상 가능), `native/cwnd_monitor.c` 빌드 완료 | 1) `pytest -m "not live" tests/core/ -v`로 `interval_from_rtt` 계산 로직 검증 2) 짧은 RTT(clean 프로파일)와 긴 RTT(3g 프로파일) 각각에서 실제 cwnd 모니터링 실행 3) idle 구간 후 IW 리셋 이벤트와 loss recovery 이벤트가 각각 올바르게 분류되는지 확인 | 170개 유닛 테스트 pass, 짧은 RTT 환경에서 샘플링 주기가 물리적 하한(1ms)에 도달 시 `floor_clamped`+`measurement_confidence: degraded`가 결과에 명시되고, idle-reset과 loss-recovery가 서로 다른 이벤트로 정확히 구분됨 |
| TC-AIPT-16 | QUIC idle-probe congestion control cwnd 사전 조정 실측 검증 | `quic-mock-server` 컨테이너 기동, Gateway `3g`/`clean` 프로파일 정의 존재 | 1) `spike_runner.py`로 `clean` 프로파일에서 baseline(reno) vs idle_probe A/B 실행 2) 동일하게 `3g` 프로파일(delay 150±40ms, loss 1%)에서 A/B 실행 3) 각 실행의 idle 구간 전후 cwnd 값 비교 | `clean` 프로파일에서는 baseline/idle_probe 간 cwnd 차이가 노이즈 수준(유의미한 차이 없음), `3g` 프로파일에서는 idle_probe가 RTT 증가(11.7~19.4%)를 감지해 cwnd를 사전 축소(예: 3188→2643)하고 baseline은 idle 구간 동안 cwnd 변화 없음 |

## 3. 비고

- 위 6개 기능은 모두 코드가 실제로 구현되어 있고, 각각 pytest 스위트(`pytest -m "not live"` 기준
  총 513개 테스트 중 일부) 또는 실컨테이너 기반 실측 스크립트로 검증이 완료된 항목만 선별했다.
- 마지막 QUIC idle-probe 항목만 "웹 UI/`RunRequest` 미연결"이라는 통합 범위 제한이 있으나, 핵심
  알고리즘 자체(`IdleProbeCongestionControl`)는 구현·실측 완료 상태이므로 포함했다.
- JIRA 실입력 시 Epic Name은 팀 컨벤션에 맞는 기존 Epic에 매핑하거나 신규 Epic으로 생성.
