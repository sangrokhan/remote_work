# AIPT — SRS(JIRA 티켓 초안) & STD(테스트 설계서) — 구현 완료 기능 기준

- 작성일: 2026-09-01 (수정판 — 미구현 갭이 아닌 **이미 구현·실측 검증된 기능**을 대상으로 재작성)
- 근거 문서: `DESIGN.md` §4.5~§4.10, `ARCHITECTURE.md` §2~§7
- 범위: 실제 코드로 구현이 완료되고 테스트/실측으로 검증된 5개 핵심 기능을 JIRA 티켓 후보(SRS 대체)와
  그에 대응하는 회귀 테스트 케이스(STD)로 정리한다. (Run Store 영속화, QUIC idle-probe 항목은 제외)

## 1. SRS — JIRA 티켓 초안 (요구사항서 대체, 구현 완료 기능)

| Epic Name | Summary | Feature Type | Description | Constraint | Validation Method | Validation |
|---|---|---|---|---|---|---|
| 3-Backend 통합 아키텍처 | PublicAI/Mock/LocalLLM 3개 backend 공통 프로토콜 구현 | New Feature | `Backend` 프로토콜(connect/send_turn/close)로 3개 backend(PublicAIBackend: 실제 Gemini/OpenAI 10개 arm, MockBackend: 고정 byte-size/Q&A record 재생, LocalLLMBackend: llama.cpp/vLLM 앞단 engine gateway 경유)를 단일 인터페이스로 통합 구현. `aipt/web`이 backend 목록/준비 상태를 동적으로 수집해 노출한다. | PublicAI만 Network Gateway를 경유하지 않음(이미 실제 인터넷이 역할 수행). 소켓 레벨 계측 가능한 요청 방식(SDK 대신 requests 기반)을 써야 함. | `tests/test_backends_base.py`(레지스트리 등록/조회) + `tests/backends/`(15개 파일, 163개 테스트, 프로토콜 준수/arm별 body 빌드) | `pytest -m "not live" tests/backends/ tests/test_backends_base.py` 전량 pass(163+11건), 3개 backend 모두 FastAPI TestClient로 실행 스모크 통과 |
| Network Gateway L3 트래픽 셰이핑 | client-link-only netem 기반 Network Gateway 구현 | New Feature | Mock/LocalLLM backend 경로에 `tc netem`으로 지연/지터/손실/재정렬을 주입하는 순수 L3 IP 포워딩 컨테이너 구현 완료. client_iface(net-client)에는 사용자가 고른 프로파일(clean/wired/wireless/custom)을 양방향 적용하고, ingress 방향은 IFB(Intermediate Functional Block)로 리다이렉트해 우회 없이 shaping. backend_iface는 항상 고정 `ETHERNET_BASELINE`만 적용. | Gateway는 TCP 상태/페이로드를 들여다보지 않는 순수 L3 forwarding이어야 함(`NET_ADMIN` + `ip_forward=1`만 사용). 런타임 프로파일 전환이 기존 keep-alive 연결에도 즉시 반영되어야 함. | `tests/gateway/`(4개 파일, 65개 테스트 — 프로파일 값 정의, tc 명령 구성 subprocess mock, API 라우트, client-link-only L3 forwarding 로직) | `pytest -m "not live" tests/gateway/` 65건 전량 pass, 실컨테이너에서 프로파일별 RTT가 설정값 근방으로 실측됨 |
| engine Gateway 요청 dedup 캐싱 | LocalLLM 멀티턴 요청 leaf-hash 중복 제거 캐싱 구현 | New Feature | 멀티턴 대화에서 매 턴 재전송되는 `messages` 텍스트를 TCP 세션(커넥션) 단위로 hash 치환해 전송량 절감. 클라이언트측(`Gateway.send()`)이 leaf 순회 후 이미 본 값을 hash로 치환, 서버측(`docker/engine_gateway.py` L7 sidecar, 포트 40079)이 세션 캐시로 원복. 캐시 미스 시 HTTP 409 + 1회 재전송. | `local_llm` backend 전용, 기본값 off, `X-AIPT-Cache: enable` 헤더로 opt-in. 200 bytes 미만 값은 캐싱 대상 제외(임계값). | `scripts/measure_perf_cache_savings.py`로 실제 4-service docker-compose 토폴로지(web→Network Gateway→engine Gateway→llama-server) 위에서 off/on 실측 비교 + `scripts/_smoketest_*.py` 4종(standalone assert) | 20턴 누적 기준 요청 payload 87.2% 절감(529,002→67,592 bytes), 실제 wire 전송량 86.3% 절감(533,942→72,952 bytes) 실측 확인, 4개 smoketest 스크립트 전량 pass |
| 적응형 cwnd 계측 인프라 | 별도 프로세스 + RTT 비례 적응형 샘플링 주기 구현 | New Feature | `aipt/core/cwnd.py`의 `Monitor`가 Python 스레드가 아닌 완전히 별도의 OS 프로세스(`native/cwnd_monitor.c`)를 기동해 자체 클록으로 netlink `sock_diag`를 폴링, GIL/이벤트루프 지연의 샘플링 왜곡을 원천 차단. 샘플링 주기는 고정값이 아니라 `interval_ms = max(1, rtt_ms / K)`로 경로 RTT에 비례해 계산(적응형), 물리적 하한(1ms) 아래는 `floor_clamped` + `measurement_confidence: "degraded"`로 명시 보고. | 짧은 RTT 경로(Mock backend clean 프로파일 등)에서 고정 주기를 쓰면 슬로우스타트 burst를 샘플러가 놓칠 수 있음 — 반드시 적응형이어야 함. | `tests/core/`(9개 파일, 170개 테스트 — cwnd reset 판정, AppArmor 감지, interval_from_rtt, timestamp_source 판별 등) | `pytest -m "not live" tests/core/` 170건 pass, idle 후 IW 리셋과 loss recovery가 서로 다른 이벤트로 정확히 구분 판정됨을 단위 테스트로 확인 |
| 3-레이어 통계/CSV 통합 | cwnd/turn/packet 3-레이어 CSV export + goodput 산출 구현 | New Feature | 3개 backend 공통으로 connection-level(`cwnd.csv`: tick별 snd_cwnd/rtt/delivery_rate), turn-level(`turns.csv`: prompt_bytes/wire_sent·recv/tokens/ttft/ttlt/turn_end/goodput), packet-level(`packets.csv`: pcap에서 추출한 inter-arrival gap·크기 분포)을 통합 구현. 세 CSV + pcap을 `bundle.zip`으로 묶어 다운로드. `aipt/export/turns.py`가 `wire_recv`와 `(turn_end_ms − req_sent_ms)`로 `goodput_bps` 산출(0-나눗셈 가드 포함). | packet-level CSV는 기존에 없던 완전 신규 구현(`dpkt` 신규 의존성 필요). 3개 backend 모두 동일 스키마를 내야 함(레이어별 컬럼 불변성). | `tests/export/`(4개 파일, 42개 테스트 — 3-레이어 CSV 스키마 불변성, goodput_bps 계산, 합성 pcap으로 dpkt/stdlib 파서 교차검증 pcap 라운드트립) | `pytest -m "not live" tests/export/` 42건 pass, 3개 backend 각각 실행 시 동일 스키마의 `cwnd.csv`/`turns.csv`/`packets.csv`가 생성되고 `bundle.zip`으로 다운로드됨을 확인 |
| Public AI 실측 캡처 → Mock 재생 파이프라인 | 실제 API 호출 원문 캡처 후 마스킹, Mock backend 재생용 record 저장 구현 | New Feature | `aipt/backends/public_ai/recorder.py`가 PublicAIBackend 호출 시 request/response 원문을 캡처해 API 키 등 민감정보를 마스킹한 뒤 Mock record 포맷(Q&A JSON)으로 저장. `aipt/backends/mock/replay.py`가 이 record를 읽어 바이트 패턴만 재생(지연시간은 재현하지 않고 `inference_delay` 설정값으로 별도 제어). | 재현 정확도보다 구현 명료성 우선 — 실제 지연 재현은 하지 않는다는 점을 결과에 명시해야 함. 마스킹 누락 시 민감정보 유출 위험이 있어 recorder 저장 경로는 반드시 마스킹을 거쳐야 함. | `tests/backends/`(public_ai/mock 하위, record/replay 왕복 테스트 포함) | Public AI 실제 호출 1건 캡처 후 저장된 record에 API 키 등 민감정보가 마스킹되어 있음을 확인, 동일 record를 Mock backend로 재생 시 바이트 패턴(payload 크기)이 원본과 일치함 |

## 2. STD — 테스트 케이스 (구현 완료 기능 회귀 검증)

| Test ID | Description | Precondition | Test Step | Expected Results |
|---|---|---|---|---|
| TC-AIPT-11 | 3개 backend 공통 프로토콜 준수 및 레지스트리 동작 검증 | venv 활성화, `tests/backends/`, `tests/test_backends_base.py` 존재 | 1) `pytest -m "not live" tests/test_backends_base.py -v` 실행 2) `pytest -m "not live" tests/backends/ -v` 실행 3) 미등록 backend 이름 조회 시 예외 확인 | `tests/test_backends_base.py` 11건, `tests/backends/` 163건 모두 pass, 미등록 이름 조회 시 명시적 예외 발생 |
| TC-AIPT-12 | Network Gateway client-link-only netem 프로파일 적용 검증 | docker-compose로 `gateway`/`mock-server` 컨테이너 기동, `wireless` 프로파일 정의 존재 | 1) `curl -X POST "http://localhost:10000/api/gateway/profile?profile=wireless"`(또는 web UI의 Gateway 설정 화면)로 `wireless` 프로파일 설정 2) `POST /api/run`으로 mock backend 실험 1건 실행(예: `curl -X POST http://localhost:10000/api/run -d '{"backend":"mock","arm":"dummy"}' -H "Content-Type: application/json"`) — `capture`(기본 `True`)와 무관하게 idle-gap ping probe(실제 모듈은 `aipt/backends/mock/probe.py`, `enable_ping_probes` 기본 `True`; 문서상 `aipt.core.probe`는 오기)가 각 턴의 idle 구간마다 자동으로 RTT를 측정함 3) RTT 확인은 두 가지 중 하나: (a) `/api/run` 실행 결과 JSON에 포함된 ping probe 샘플의 `rtt_ms` 값을 읽거나, (b) `capture=true`로 저장된 pcap(`data/pcaps/`)을 열어 SYN-ACK 왕복시간을 직접 측정 — 별도로 실행하는 스크립트는 없음 4) `GET /api/gateway/profile` 응답의 `backend.profile` 필드가 항상 `ethernet_baseline`인지 확인 | client_iface 구간 RTT가 `wireless` 프로파일 delay 설정값 근방으로 관측되고, backend_iface 구간은 프로파일 선택과 무관하게 baseline(약 1ms)으로 유지됨 |
| TC-AIPT-13 | engine Gateway 캐싱 트래픽 절감 실측 재현 검증 | docker-compose 4-service 토폴로지 기동, `records/perf.json` 존재(스크립트 기본 경로, `PERF_RECORD_PATH` 환경변수로 변경 가능) | 1) `docker compose exec web python3 scripts/measure_perf_cache_savings.py` 1회 실행 — off/on은 CLI 인자가 아니라 스크립트 내부 `run_conversation()` 호출이 `cache_enabled=False`(baseline, `X-AIPT-Cache` 헤더 미전송) 후 `cache_enabled=True`(cached, `X-AIPT-Cache: enable` 헤더 전송) 순서로 자동 2회 수행함 2) `data/runs/cache_savings_multiturn.csv`에서 턴별 `req_payload_bytes`/`wire_sent` 비교 3) `python3 scripts/_smoketest_cache_miss_recovery.py` 실행 4) `python3 scripts/_smoketest_gateway_409_path.py` 실행 | 20턴 누적 payload 절감률 87.2%, wire 전송량 절감률 86.3% 근방 재현, 캐시 미스(409) 발생 시 클라이언트가 자기 캐시로 원본 복원 후 1회 재전송에 성공, 두 smoketest 스크립트 모두 pass |
| TC-AIPT-14 | 3종 CSV export(연결 단위 `cwnd.csv` / 턴 단위 `turns.csv` / 패킷 단위 `packets.csv`)가 PublicAI·Mock·LocalLLM 어느 backend로 실행해도 동일한 컬럼 구성을 유지하는지, 그리고 `turns.csv`의 `goodput_bps` 계산값이 수식과 일치하는지 검증 | venv 활성화, `tests/export/` 존재, 3개 backend 실행 가능 환경 | 1) `pytest -m "not live" tests/export/ -v` 실행 2) Mock backend로 실험 1건 실행 후 `cwnd.csv`/`turns.csv`/`packets.csv`/`bundle.zip` 생성 확인 3) PublicAI/LocalLLM backend로도 동일 실행해 3개 CSV 스키마(컬럼) 동일한지 비교 4) `turns.csv`의 `goodput_bps` 값이 `wire_recv / (turn_end_ms - req_sent_ms)` 계산과 일치하는지 확인 | 42개 유닛 테스트 pass, 3개 backend 모두 동일 컬럼 스키마의 CSV 3종을 생성하고 `bundle.zip`에 pcap과 함께 포함됨, `goodput_bps` 계산값이 수식과 일치하며 0-나눗셈 가드가 정상 동작(turn_end_ms == req_sent_ms인 경우 예외 없이 처리) |
| TC-AIPT-15 | Public AI 실측 캡처 → Mock 재생 파이프라인 마스킹/재현 검증 | Public AI API 키 설정 완료, `aipt/backends/public_ai/recorder.py`/`aipt/backends/mock/replay.py` 구현 완료 | 1) PublicAIBackend로 실제 API 호출 1건 실행해 recorder가 record 저장 2) 저장된 record 파일을 열어 API 키/인증 헤더 등 민감정보가 마스킹되어 있는지 확인 3) 동일 record를 Mock backend의 replay 경로로 로드해 재생 실행 4) 재생된 응답의 payload 바이트 크기를 원본 캡처와 비교 | record에 민감정보(API 키, Authorization 헤더 등)가 마스킹되어 원문 그대로 노출되지 않고, Mock 재생 시 payload 바이트 패턴(크기)이 원본과 일치(단, 지연시간은 `inference_delay` 설정값으로 별도 제어되어 원본 지연과 다를 수 있음이 기대대로 확인됨) |
| TC-AIPT-16 | 적응형 cwnd 계측 인프라(별도 프로세스 + RTT 비례 샘플링) 검증 | venv 활성화, `tests/core/` 존재, `native/cwnd_monitor.c` 빌드 가능 환경 | 1) `pytest -m "not live" tests/core/ -v` 실행 — 아래 2~5번은 별도 절차가 아니라 이 한 번의 pytest 실행에 포함된 서로 다른 테스트 함수들의 결과를 각각 확인하는 것 2) idle 후 IW 리셋 vs loss recovery 구분 판정: `tests/core/test_cwnd.py::test_a_grown_window_falling_back_to_ten_counts_as_a_reset`(리셋 1회로 판정)와 `::test_a_collapse_during_loss_recovery_is_not_counted`(리셋 0회로 판정) 두 테스트의 pass 여부 확인 3) RTT 비례 적응형 샘플링 주기 계산: `tests/core/test_cwnd.py::test_interval_from_rtt_normal_path`(10ms→2ms), `::test_interval_from_rtt_scales_with_rtt`(100ms→20ms) 등 `interval_from_rtt()` 관련 테스트들의 pass 여부 확인 4) 물리적 하한(1ms) 클램프 + degraded confidence 보고: `::test_interval_from_rtt_very_short_path_hits_the_floor`, `::test_monitor_with_short_rtt_hint_clamps_and_degrades_confidence`의 pass 여부 확인 5) timestamp_source 판별: `tests/core/test_capture.py::test_timestamp_source_reports_hardware_when_ethtool_says_so`, `::test_timestamp_source_reports_software_only` 등의 pass 여부 확인 | `tests/core/` 170건 전량 pass, idle 후 IW 리셋과 loss recovery가 서로 다른 이벤트로 정확히 구분 판정되며, 짧은 RTT 경로에서도 `floor_clamped` 하한 클램프와 `degraded` confidence 보고가 정상 동작함 |

## 3. 비고

- 위 6개 기능은 모두 코드가 실제로 구현되어 있고, 각각 pytest 스위트(`pytest -m "not live"` 기준
  총 513개 테스트 중 일부) 또는 실컨테이너 기반 실측 스크립트로 검증이 완료된 항목만 선별했다.
- SRS 6개 항목과 STD 6개 테스트 케이스(TC-AIPT-11~16)가 1:1로 매핑되도록 구성했다
  (기존 버전에서는 "적응형 cwnd 계측 인프라" 항목에 대응하는 STD가 누락되어 있었음 — TC-AIPT-16으로 보완).
- 요청에 따라 Run Store 영속화, QUIC idle-probe 항목은 이번 버전에서 제외했다.
- JIRA 실입력 시 Epic Name은 팀 컨벤션에 맞는 기존 Epic에 매핑하거나 신규 Epic으로 생성.
