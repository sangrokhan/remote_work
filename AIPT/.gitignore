__pycache__/
*.pyc
.venv/
.pytest_cache/
*.egg-info/

# Build artifacts
native/cwnd_monitor

# Runtime data (pcaps, run history) -- not source
data/

# Local secrets/overrides (API keys) -- generated from .env.example by
# scripts/ensure_env.sh / `make up`, never checked in.
.env
