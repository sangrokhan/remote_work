# Neo4j Configuration
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password

# LLM Configuration
LLM_API_BASE=http://localhost:8000/v1
LLM_API_KEY=EMPTY
LLM_MODEL=models/gemma-2-2b-it
LLM_TIMEOUT=60.0

# Graph Construction Chunking
CHUNK_SIZE=1000
CHUNK_OVERLAP=200

# Ontology Discovery Chunking
ONTOLOGY_CHUNK_SIZE=1000
ONTOLOGY_CHUNK_OVERLAP=200

# Ontology Configuration
ONTOLOGY_PATH=ontology.json

# Discovery Configuration
ONTOLOGY_DISCOVERY_PROMPT="You are a 3GPP Ontology Architect. \nAnalyze the technical text and extract a formal schema.\n\nInstructions:\n1. Identify Generic Node Types. For each type, define its 'properties' (what attributes are mentioned for this category?).\n2. Identify Relationship Types. For each, define its 'properties' (what metadata describes this link, like 'condition' or 'interface'?).\n3. Use specific categories: NetworkNode, ProtocolMessage, Timer, Procedure, UserState.\n\nReturn STRICTLY a JSON object:\n{{\n  \"node_types\": [\n    {{\n      \"label\": \"Label\", \n      \"description\": \"Definition\", \n      \"properties\": {{\"prop_name\": \"type/desc\"}}\n    }}\n  ],\n  \"relationship_types\": [\n    {{\n      \"source\": \"Label\", \n      \"type\": \"REL_TYPE\", \n      \"target\": \"Label\", \n      \"properties\": {{\"prop_name\": \"type/desc\"}}\n    }}\n  ]\n}}\n\nText:\n{text}\n\nJSON Output:"

# Extraction Prompt
TRIPLE_EXTRACTION_PROMPT="You are a 3GPP Knowledge Graph expert. \nExtract knowledge triples strictly following the provided ONTOLOGY.\n\nONTOLOGY:\n{ontology}\n\nInstructions:\n1. Map specific entities to Generic Node Labels.\n2. Populate 'node_properties' and 'edge_properties' based on the schema.\n3. Return strictly a JSON object with a key 'triples'.\n\nExample Triple:\n{{\n  \"head\": \"UE\",\n  \"head_label\": \"NetworkNode\",\n  \"head_properties\": {{\"id\": \"UE1\", \"state\": \"IDLE\"}},\n  \"type\": \"SENDS\",\n  \"edge_properties\": {{\"interface\": \"Uu\", \"condition\": \"Initial access\"}},\n  \"tail\": \"gNB\",\n  \"tail_label\": \"NetworkNode\"\n}}\n\nText:\n{text}\n\nJSON Output:"

# Consolidation Prompt
ONTOLOGY_CONSOLIDATION_PROMPT="Consolidate the following raw extractions into a single, unified 3GPP ontology. \nMerge similar properties and ensure every node/relationship has a clear definition.\n\nNodes:\n{nodes}\n\nRelationships:\n{relationships}\n\nReturn the unified JSON schema."
